# actionlint config
#
# Whitelist self-hosted runner labels used by the org's runners. The other
# labels (self-hosted, linux, arm64) are already known to actionlint.
self-hosted-runner:
  labels:
    - oracle
    - marketplace-publisher
